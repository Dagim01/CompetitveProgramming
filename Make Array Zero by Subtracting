class Solution:
    def minimumOperations(self, nums: List[int]) -> int:
        if nums.count(0)==len(nums):
            return 0
        n = set(nums)
        if 0 in n:
            x=1
        else: 
            x=0
        return len(n) - x
